    /*_.map(data.data, function(row){
              row.Tags = row.Tags.split("|");
              
              row.Hours = [];
              for(var i = 0; i < 24; i++) {
                row.Hours.push(Math.ceil(2000 + Math.random() * 3000));
              }
              return row;
            }); */
          
          //console.log($scope.dataRows);
          
            //console.log(JSON.stringify($scope.dataRows));